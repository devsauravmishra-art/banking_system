# main.py Documentation

## Overview
The `main.py` file is the entry point of the banking system application. It handles user interactions and manages the flow of the application.

## Functionality
- User authentication
- Account management
- Transaction processing

## Usage
To run the application, execute the following command:
```
python main.py
```

## Requirements
- Python 3.x
- Required libraries (listed in requirements.txt)